{
  "name": "maskpoint-claude-code",
  "version": "0.4.0",
  "description": "Maskpoint: masks stale tool observations into placeholders before Claude Code compacts, and re-injects the artifact afterward. Assisted augmentation \u2014 the host's own compactor is not replaced."
}
